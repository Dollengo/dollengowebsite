---
title: 'DollOS'
publishedAt: '2024-08-07'
summary: 'DollOS, a software to help Linux beginners'
---

# Explain DollOS

### [github.com/dollengo/dollos](https://github.com/dollengo/dollos)

## Table of Contents

- [Introduction](#introduction)
- [Dependencies](#dependencies)
- [Setup](#setup)
- [Commands](#commands)
- [Modules](#modules)
- [Functions](#functions)

## Introduction

DollOS is a command-line based operating system emulator created by Dollengo (Tarcísio). This project provides various system commands and functionalities, simulating an operating system environment. 
Under licensed by GPL-3.0 license

## Dependencies

The following Python packages are required to run DollOS:

- `random`
- `binrobotdollos`
- `morsildollos`
- `dollpycode`
- `dollnotepad`
- `rich`
- `subprocess`
- `psutil`
- `json`
- `shutil`
- `datetime`
- `time`

## Setup

1. Clone the repository from GitHub:

    ```bash
    git clone https://github.com/dollengo/dollos.git
    cd dollos
    ```

2. Install the necessary dependencies:

    ```bash
    pip install rich psutil
    ```

3. Run the `start()` function to begin:

    ```bash
    python dollos.py
    ```

## Commands

DollOS supports a variety of commands. Use the `.help` command to display the list of available commands. Some key commands include:

- `.desktop`: Show the Desktop
- `.help`: Show help for using the operating system
- `.powershell`: Execute Windows PowerShell commands
- `.exit`: Exit the system
- `.list`: List files in the current directory
- `.read [filename]`: Read a file
- `.write [filename]`: Write to a file
- `.rename [old_name] [new_name]`: Rename a file
- `.mkdir [dirname]`: Create a directory
- `.rmdir [dirname]`: Remove a directory
- `.delete [filename]`: Delete a file
- `.move [src] [dest]`: Move a file
- `.copy [src] [dest]`: Copy a file
- `.cd [dirname]`: Change directory
- `.datetime`: Display date and time
- `.history`: Show command history
- `.usage`: Show CPU and memory usage
- `.netstat`: Show network statistics
- `.ps`: Show running processes
- `.kill [pid]`: Kill a process by its PID
- `.touch [filename]`: Create an empty file
- `.cat [filename]`: Display file content
- `.head [filename] [n]`: Display the first `n` lines of a file
- `.tail [filename] [n]`: Display the last `n` lines of a file
- `.du`: Display disk usage
- `.uptime`: Show system uptime
- `.env`: Show environment variables
- `.setenv [key] [value]`: Set an environment variable
- `.unsetenv [key]`: Unset an environment variable
- `.path`: Show the system PATH
- `.addpath [path]`: Add a directory to the system PATH
- `.removepath [path]`: Remove a directory from the system PATH
- `.ports`: List open ports
- `.ipconfig`: Show IP configuration
- `.ping [host]`: Ping a host
- `.traceroute [host]`: Trace the route to a host
- `.hostname`: Show the hostname
- `.arp`: Show the ARP table
- `.route`: Show the routing table
- `.systeminfo`: Show system information
- `.diskpart`: Manage disk partitions
- `.mount [device] [path]`: Mount a device
- `.umount [path]`: Unmount a device
- `.chmod [mode] [filename]`: Change file permissions
- `.chown [user:group] [filename]`: Change file owner
- `.lsusb`: List USB devices
- `.lspci`: List PCI devices
- `.dmesg`: Display system messages
- `.date`: Display the date
- `.time`: Display the time
- `.man [command]`: Show the manual for a command
- `.alias [name] [command]`: Create an alias
- `.unalias [name]`: Remove an alias
- `.su [user]`: Switch user
- `.sudo [command]`: Execute a command as another user
- `.install [package]`: Install a package
- `.uninstall [package]`: Uninstall a package
- `.update`: Update packages
- `.upgrade`: Upgrade packages
- `.services`: List services
- `.start [service]`: Start a service
- `.stop [service]`: Stop a service
- `.restart`: Restart the system
- `.enable [service]`: Enable a service
- `.disable [service]`: Disable a service
- `.status [service]`: Display service status
- `.log [service]`: Display service log
- `.journal`: Show the system journal
- `.save [filename]`: Save the current state
- `.load [filename]`: Load a saved state
- `.shutdown`: Shutdown the system
- `.reboot`: Reboot the system
- `.ldi`: List device information
- `.errors`: List error codes
- `.dolley`: Create a new text file with encrypted data
- `.opendolley`: Read the dolley file
- `.savestate`: Save the current state (aliases, desktoparray, etc.)
- `.loadstate`: Load the saved state

## Modules

The system is organized into several modules for better functionality and maintainability. Here is an overview of the modules used:

- `binrobotdollos`: Custom module for DollOS
- `morsildollos`: Custom module for DollOS
- `dollpycode`: Custom module for DollOS
- `dollnotepad`: Custom module for DollOS

## Functions

### Core Functions

- `clear_console()`: Clears the console screen.
- `save_state()`: Saves the current state of the system.
- `load_state()`: Loads a previously saved state of the system.
- `start()`: Displays the welcome message for DollOS.
- `show_help()`: Displays the help message.
- `execute_powershell()`: Executes PowerShell commands.
- `exit_system()`: Exits the system.
- `list_files()`: Lists files in the current directory.
- `read_file(filename)`: Reads the content of a file.
- `write_file(filename)`: Writes content to a file.
- `rename_file(old_name, new_name)`: Renames a file.
- `create_directory(dirname)`: Creates a new directory.
- `remove_directory(dirname)`: Removes a directory.
- `delete_file(filename)`: Deletes a file.
- `move_file(src, dest)`: Moves a file from source to destination.
- `copy_file(src, dest)`: Copies a file from source to destination.
- `change_directory(dirname)`: Changes the current directory.
- `display_datetime()`: Displays the current date and time.
- `show_history()`: Shows the command history.
- `show_usage()`: Displays the CPU and memory usage.
- `show_netstat()`: Displays network statistics.
- `show_processes()`: Shows running processes.
- `kill_process(pid)`: Kills a process by its PID.
- `create_empty_file(filename)`: Creates an empty file.
- `display_file_content(filename)`: Displays the content of a file.
- `display_file_head(filename, n)`: Displays the first `n` lines of a file.
- `display_file_tail(filename, n)`: Displays the last `n` lines of a file.
- `display_disk_usage()`: Displays disk usage.
- `show_uptime()`: Shows the system uptime.
- `show_env()`: Shows environment variables.
- `set_env(key, value)`: Sets an environment variable.
- `unset_env(key)`: Unsets an environment variable.
- `show_path()`: Shows the system PATH.
- `add_path(path)`: Adds a directory to the system PATH.
- `remove_path(path)`: Removes a directory from the system PATH.
- `list_open_ports()`: Lists open ports.
- `show_ipconfig()`: Shows IP configuration.
- `ping_host(host)`: Pings a host.
- `traceroute_host(host)`: Traces the route to a host.
- `show_hostname()`: Shows the hostname.
- `show_arp()`: Shows the ARP table.
- `show_route()`: Shows the routing table.
- `show_systeminfo()`: Shows system information.
- `manage_diskpart()`: Manages disk partitions.
- `mount_device(device, path)`: Mounts a device.
- `unmount_device(path)`: Unmounts a device.
- `change_permissions(mode, filename)`: Changes file permissions.
- `change_owner(user_group, filename)`: Changes file owner.
- `list_usb()`: Lists USB devices.
- `list_pci()`: Lists PCI devices.
- `display_dmesg()`: Displays system messages.
- `display_date()`: Displays the current date.
- `display_time()`: Displays the current time.
- `show_man(command)`: Shows the manual for a command.
- `create_alias(name, command)`: Creates an alias.
- `remove_alias(name)`: Removes an alias.
- `switch_user(user)`: Switches user.
- `execute_as_sudo(command)`: Executes a command as another user.
- `install_package(package)`: Installs a package.
- `uninstall_package(package)`: Uninstalls a package.
- `update_packages()`: Updates packages.
- `upgrade_packages()`: Upgrades packages.
- `list_services()`: Lists services.
- `start_service(service)`: Starts a service.
- `stop_service(service)`: Stops a service.
- `restart_system()`: Restarts the system.
- `enable_service(service)`: Enables a service.
- `disable_service(service)`: Disables a service.
- `show_service_status(service)`: Displays the status of a service.
- `show_service_log(service)`: Displays the log of a service.
- `show_journal()`: Shows the system journal.
- `create_dolley_file(filename)`: Creates a new text file with encrypted data.
- `read_dolley_file(filename)`: Reads the dolley file.
- `save_state()`: Saves the current state (aliases, desktoparray, etc.).
- `load_state()`: Loads the saved state.
